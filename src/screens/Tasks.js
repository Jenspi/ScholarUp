import React from "react";
import { Text, StyleSheet } from "react-native";

const Tasks = () => {
  return <Text style={styles.text}>Tasks</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default Tasks;
