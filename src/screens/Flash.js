import React from "react";
import { Text, StyleSheet } from "react-native";

const FlashCard = () => {
  return <Text style={styles.text}>Jumana Suleiman</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default FlashCard;
