import React from "react";
import { Text, StyleSheet } from "react-native";

const SignUp = () => {
  return <Text style={styles.text}>Sign Up</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default SignUp;
